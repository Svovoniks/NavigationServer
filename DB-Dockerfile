FROM postgres
ARG DB_NAME
ARG POSTGRES_USER
ARG POSTGRES_PASSWORD
ARG POSTGRES_HOST
ENV DB_NAME=${ROUTING_DB_NAME}
ENV POSTGRES_USER=${POSTGRES_USER}
ENV POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
ENV POSTGRES_HOST=${POSTGRES_HOST}
COPY /sql-scripts/DB_setup.sql /docker-entrypoint-initdb.d/